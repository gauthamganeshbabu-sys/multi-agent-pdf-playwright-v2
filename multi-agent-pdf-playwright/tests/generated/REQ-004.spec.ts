import { test, expect } from '@playwright/test';

/**
 * REQ-004: User should click login button.
 * Type: Positive | Priority: Low
 * Auto-generated by Multi-Agent PDF-Playwright Framework
 */

test.describe('REQ-004 - User should click login button.', () => {
  test.afterEach(async ({ page }, testInfo) => {
    if (testInfo.status !== testInfo.expectedStatus) {
      await page.screenshot({ path: `reports/screenshots/${testInfo.title.replace(/[^a-z0-9]/gi, '_').substring(0,50)}.png` });
    }
  });

  test('User should click login button.', async ({ page }) => {
    const appUrl = process.env.APP_URL || "https://www.saucedemo.com";
    await page.goto(appUrl);


    await page.locator("input[type=\"submit\"], button[type=\"submit\"], button:has-text(\"User should click login .\")")
      .first()
      .click({ timeout: 10000 });

    await expect(page.locator('html')).toBeVisible();
  });
});