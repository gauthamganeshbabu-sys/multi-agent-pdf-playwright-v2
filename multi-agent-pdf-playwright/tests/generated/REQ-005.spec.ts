import { test, expect } from '@playwright/test';

/**
 * REQ-005: After successful login user should navigate to product page. Validation: Verify products page is displayed successfully.
 * Type: Positive | Priority: Low
 * Auto-generated by Multi-Agent PDF-Playwright Framework
 */

test.describe('REQ-005 - After successful login user should navigate to product page. Validation: Verify products page is displayed successfully.', () => {
  test.afterEach(async ({ page }, testInfo) => {
    if (testInfo.status !== testInfo.expectedStatus) {
      await page.screenshot({ path: `reports/screenshots/${testInfo.title.replace(/[^a-z0-9]/gi, '_').substring(0,50)}.png` });
    }
  });

  test('After successful login user should navigate to product page. Validation: Verify products page is displayed successfully.', async ({ page }) => {
    const appUrl = process.env.APP_URL || "https://www.saucedemo.com";
    await page.goto(appUrl);

    await page.goto(appUrl);
    await page.waitForLoadState('domcontentloaded');
    await expect(page.locator('html')).toBeVisible();
  });
});